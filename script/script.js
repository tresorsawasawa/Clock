setInterval(setClock, 1000);

function setClock() {
    const currentDate = new Date(),
        secondsRatio = currentDate.getSeconds() / 60,
        secondsRatio = currentDate.getSeconds() / 60,
        secondsRatio = currentDate.getSeconds() / 60;
}